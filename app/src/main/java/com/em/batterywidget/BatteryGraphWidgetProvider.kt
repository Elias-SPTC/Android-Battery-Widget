package com.em.batterywidget

import android.appwidget.AppWidgetProvider

/**
 * ARQUIVO OBSOLETO. A lógica foi consolidada no BatteryAppWidgetProvider e no BatteryWorker.
 * Este arquivo é mantido vazio para evitar erros de compilação, mas não é mais usado.
 */
class BatteryGraphWidgetProvider : AppWidgetProvider() {}
