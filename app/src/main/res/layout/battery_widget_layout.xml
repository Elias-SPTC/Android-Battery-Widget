<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/widget_margin"
    android:background="@drawable/widget_background_rounded">

    <!-- O bloco TextView para a Porcentagem, centralizado. -->
    <TextView
        android:id="@+id/battery_percentage_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:text="--%" />

    <!-- O bloco ImageView para o Ícone de Status (Corrigido para fechar com />) -->
    <ImageView
        android:id="@+id/status_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/battery_percentage_text"
        android:layout_marginEnd="8dp"
        android:contentDescription="@string/content_description_status_icon"
        android:src="@drawable/ic_battery_status" />

    <!-- O bloco TextView para o Status de Carregamento/Saúde -->
    <TextView
        android:id="@+id/status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/battery_percentage_text"
        android:layout_centerHorizontal="true"
        android:textSize="10sp"
        android:textColor="@color/widget_secondary_text"
        android:text="@string/status_unknown" />

</RelativeLayout>