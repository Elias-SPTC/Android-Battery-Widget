<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_text_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background_dark">

    <!-- Barra de Progresso estilizada como uma pilha -->
    <ProgressBar
        android:id="@+id/battery_progress_bar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:progressDrawable="@drawable/progress_bar_horizontal_battery_gradient"
        tools:progress="85" />

    <!-- Texto da Porcentagem, centralizado sobre a barra -->
    <TextView
        android:id="@+id/text_level_only"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textColor="#FFFFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        tools:text="85%" />

    <!-- Ícone de Raio, sobreposto e inicialmente invisível -->
    <ImageView
        android:id="@+id/charging_bolt_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_charging_bolt"
        android:visibility="gone"
        tools:visibility="visible" />

</FrameLayout>
