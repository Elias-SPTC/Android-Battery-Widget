<?xml version="1.0" encoding="utf-8"?>
<!--
  Layout principal para o BatteryWidget.
  Utiliza um RelativeLayout para posicionar os elementos de forma flexível.
-->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    android:background="@drawable/widget_background">

    <!-- Ícone Principal da Bateria -->
    <ImageView
        android:id="@+id/image_battery_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/battery_icon_description"
        android:src="@drawable/ic_battery_full"
        tools:tint="@color/color_default"/>

    <!-- Contentor para o Nível e Status de Carga -->
    <LinearLayout
        android:id="@+id/layout_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/image_battery_icon"
        android:layout_marginStart="8dp"
        android:orientation="vertical"
        android:layout_centerVertical="true">

        <!-- Nível de Percentagem da Bateria -->
        <!-- ID: text_battery_percent (Mudou de text_battery_level!) -->
        <TextView
            android:id="@+id/text_battery_percent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/widget_text_primary"
            tools:text="85%" />

        <!-- Status da Bateria (A Carregar/Descarregando/etc) -->
        <!-- ID: text_battery_status -->
        <TextView
            android:id="@+id/text_battery_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/widget_text_secondary"
            tools:text="A descarregar" />

    </LinearLayout>

    <!-- Detalhes da Bateria (Saúde, Temperatura, Voltagem, Tecnologia) -->
    <!-- ID: text_details -->
    <TextView
        android:id="@+id/text_details"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="8dp"
        android:gravity="end"
        android:textSize="10sp"
        android:textColor="@color/widget_text_secondary"
        tools:text="Saúde: Boa\nTemp: 25.5°C\nVoltagem: 3.8V\nTecnologia: Li-ion" />

    <!-- Carimbo de Data/Hora da Última Atualização (no fundo do widget) -->
    <!-- ID: text_timestamp -->
    <TextView
        android:id="@+id/text_timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="4dp"
        android:textSize="8sp"
        android:textColor="@color/widget_text_secondary"
        tools:text="Atualizado: 10:30:15" />

</RelativeLayout>