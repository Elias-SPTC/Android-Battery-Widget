<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fundo escuro referenciando o Drawable que acabamos de criar -->
    <item android:drawable="@drawable/widget_background_dark" />

    <!-- Placeholder para o texto "100%" no centro (retângulo simples) -->
    <item android:gravity="center">
        <shape android:shape="rectangle">
            <size android:width="40dp" android:height="20dp" />
            <solid android:color="#90FFFFFF" />
            <corners android:radius="4dp" />
        </shape>
    </item>
</layer-list>